# Usage

After installing dependencies, you can run the example CLI to compute a CLIP similarity between an image and a prompt:

```bash
python examples\run_example.py --image path/to/image.jpg --prompt "A cat sitting on a couch"
```

Planned features:
- Region-level scoring
- Attention visualizations
- Batch evaluation and reporting
